const name = "Whatever";
if (name === "Cristian") {
  console.log("Bienvenidx, " + name);
} else if (name === "Otro Nombre") {
  console.log("Bienvenidx, " + name);
} else {
  console.log(
    "Lo siento pero el usuario que has introducido no está registrado"
  );
}
